# Codex 技能级元数据（族群化 member：不允许隐式调用，仅 $<name> 显式触发）
# 由 scripts/family-apply.py 生成；SKILL.md description 变更后需重跑刷新。
interface:
  display_name: "Design Diagram"
  short_description: "Artifact 内联 SVG 图表绘制规范：架构图、流程图、机制图、状态图、对比图，展示真实机制且在明暗主题下都清晰可读"
policy:
  allow_implicit_invocation: false
