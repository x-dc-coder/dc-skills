# Codex 技能级元数据（族群化 member：不允许隐式调用，仅 $<name> 显式触发）
# 由 scripts/family-apply.py 生成；SKILL.md description 变更后需重跑刷新。
interface:
  display_name: "DSH UI Optimization"
  short_description: "DSH 插件前端优化（薄壳编排 skill）：优化 DSH client 插件注入的界面（conversation.view"
policy:
  allow_implicit_invocation: false
