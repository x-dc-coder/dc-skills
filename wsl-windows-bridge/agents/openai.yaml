# Codex 技能级元数据（族群化 member：不允许隐式调用，仅 $<name> 显式触发）
# 由 scripts/family-apply.py 生成；SKILL.md description 变更后需重跑刷新。
interface:
  display_name: "Wsl Windows Bridge"
  short_description: "WSL→Windows 跨边界框架：三层调用通道（pythonw.exe 直调无弹窗首选 / cmd.exe fallback"
policy:
  allow_implicit_invocation: false
